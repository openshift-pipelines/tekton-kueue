You are analyzing a Go dependency update to assess its impact on a codebase. This is a **{{BUMP_TYPE}}** version bump.

**PR Title**: {{PR_TITLE}}

## Dependency Update Context

{{PACKAGE_CONTEXT}}

## Known High-Risk Patterns

The context may include a `riskHints` field with warnings about known high-risk patterns detected in this PR. If present, you MUST factor these into your risk assessment. These patterns are based on real incidents in this project:

- **GO_TOOLCHAIN_UPDATE**: Go build toolchain image changes frequently cause CI failures because the new Go version may not match the version used in Tekton pipeline build tasks. This almost always requires coordinated changes to build infrastructure. Default to HIGH risk.
- **GO_VERSION_BUMP**: Changes to the `go` directive in go.mod can break CI builds if the build image uses an older Go version. Default to at least MEDIUM risk.
- **CONTAINER_IMAGE_UPDATE**: Base image changes can affect build behavior and binary compatibility. Evaluate carefully.

## Your Task

Analyze the dependency update and produce a risk assessment. For each package in the context:

1. **Understand the changes**: Read the changelog/release notes and identify what changed in the dependency.
2. **Assess our usage**: Look at the source code snippets to understand how we use this dependency.
3. **Check for breaking changes**: Determine if any breaking changes in the update affect our specific usage patterns.
4. **Evaluate test coverage**: Note whether the affected files have tests.
5. **Check risk hints**: If the context includes `riskHints`, factor these known high-risk patterns into your assessment. These take precedence over what the changelog alone suggests.

## Output Format

Respond with ONLY the following markdown structure (no preamble, no extra text):

### Risk Level: [LOW | MEDIUM | HIGH]

### Summary of Dependency Changes
[2-3 sentences describing what changed in the dependency update]

### Affected Code
[For each affected file, explain what it uses from the dependency and whether the update impacts that usage. If no direct imports exist, state that.]

### Breaking Change Assessment
[Assess whether any breaking changes apply to our usage. Cite specific functions, types, or APIs from the snippets provided. If this is a minor bump, note that breaking changes are unlikely per semver.]

### Recommended Action
[One of:
- **Merge**: Low risk, no breaking changes affect our usage
- **Review specific areas**: Medium risk, reviewer should check [specific files/areas]
- **Needs careful review**: High risk, breaking changes may affect our usage]

---
> This analysis was generated by AI and may contain inaccuracies. Always verify the assessment against the actual dependency changelog and your code before merging.
